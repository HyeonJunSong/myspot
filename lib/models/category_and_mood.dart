
class category{
  bool inActivated;
  String emoji;
  String categoryName;

  category(this.inActivated, this.emoji, this.categoryName);
}

class tag{
  bool inActivated;
  String emoji;
  String moodName;

  tag(this.inActivated, this.emoji, this.moodName);
}